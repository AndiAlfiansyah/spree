<% content_for :page_title do %>
  <%= link_to plural_resource_name(Spree::PromotionItem), spree.admin_promotion_items_url %> /
  <%= Spree.t(:new_promotion_item) %>
<% end %>

<%= form_for :promotion_item, url: collection_url do |f| %>
  <fieldset>
    <%= render partial: 'form', locals: { f: f } %>
    <%= render partial: 'spree/admin/shared/new_resource_links' %>
  </fieldset>
<% end %>
